// Stock Intelligence servie SOUS /admin (on ne quitte plus le dashboard admin
// vers /ops). Réutilise exactement la même vue que /ops/stock — zéro duplication.
export { default, dynamic } from '../../ops/stock/page';
